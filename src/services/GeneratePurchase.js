module.exports.exec = (userId, concept, amount) => {
    'use strict';

    //Get the stamps card related to the business where the user is the owner.
    // BusinessByOwner -> StampsCard (get the first one for the moment)
    //

    //Create the purchase
    // userID not provided.
    //createPurchase(amount: Float!, stamps: Int!, concept: String, stampId: ID!): Purchase!

    return {
      id: 2,
      amount: 45.5,
      stamps: 5
    };
};
